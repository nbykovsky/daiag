Generate a friendly and personalized greeting message for ${GREETING_NAME}.

The greeting should be warm, welcoming, and approximately 2-3 sentences long.

Save the greeting message to the file: ${GREETING_PATH}

Make sure the parent directory exists before writing (create it if needed).

Return ONLY a JSON object (no other text before or after):
{"message": "<the greeting text you wrote>", "character_count": <number of characters in the greeting>}
